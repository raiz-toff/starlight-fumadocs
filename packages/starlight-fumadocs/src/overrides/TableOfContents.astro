---
import TableOfContentsList from './TableOfContentsList.astro';

const { toc } = Astro.locals.starlightRoute;
---

{
	toc && (
		<starlight-toc class="fuma-toc" data-min-h={toc.minHeadingLevel} data-max-h={toc.maxHeadingLevel}>
			<nav aria-labelledby="starlight__on-this-page">
				<h2 id="starlight__on-this-page" class="desktop-toc-header">{Astro.locals.t('tableOfContents.onThisPage')}</h2>
				<div class="toc-list">
					<TableOfContentsList toc={toc.items} />
				</div>
			</nav>
		</starlight-toc>
	)
}

<script src="./starlight-toc.ts"></script>

<style>
	.fuma-toc nav {
		position: relative;
	}

	/* TOC List Wrapper */
	.toc-list {
		border-inline-start: 1px solid var(--sl-color-hairline);
	}

	@media (min-width: 1280px) {
		.toc-list {
			position: relative;
			border-inline-start: none;
		}
	}

	.fuma-toc :global(ul) {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.fuma-toc :global(li) {
		margin: 0;
	}

	/* Nested TOC levels */
	.fuma-toc :global(ul ul) {
		padding-inline-start: 0.875rem;
	}
</style>
